{
  "name": "stardew-wedding-template",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173 --strictPort",
    "test": "node --test tests/static-site.test.js",
    "build": "vite build --base=/wedding/",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "verify": "npm test && npm run build && node scripts/verify-dist.js"
  },
  "devDependencies": {
    "vite": "^7.1.1"
  }
}
